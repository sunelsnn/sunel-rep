#!/usr/bin/perl
 

my $range = 10000;
my $random_number = int rand($range);
open(fileran, ">>userlist1.txt");

for ( $i=0;$i<100; $i++)
{
$random_number = int rand($range);
print fileran "$random_number\n" ;
}
close(fileran) ;
